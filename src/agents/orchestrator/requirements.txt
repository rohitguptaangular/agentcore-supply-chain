# Versions pinned to what this agent was built and tested against. mcp 2.x
# renamed streamablehttp_client, which broke an unpinned rebuild — pin, and
# upgrade deliberately.
bedrock-agentcore==1.23.0
strands-agents==1.55.1
mcp==2.1.1
boto3>=1.40
